name: highlight_sevice
description: Find and replace your link, tag or hashtag.
version: 0.0.7
homepage: 

environment:
  sdk: '>=3.1.1 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  collection: ^1.17.2
  flutter:
    sdk: flutter
  lints: any
  plugin_platform_interface: ^2.0.2
  url_launcher: ^6.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/
