name: highlight_sevice
description: A new Flutter plugin project.
version: 0.0.1
homepage:

environment:
  sdk: '>=3.1.1 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  collection: ^1.17.2
  flutter:
    sdk: flutter
  lints: ^2.1.1
  plugin_platform_interface: ^2.0.2
  url_launcher: ^6.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
